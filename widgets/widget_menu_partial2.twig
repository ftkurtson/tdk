</ol>

{% set count = 0 %}
{% set doLoop = true %}
{% set currentIndex = loop.index %}
{% set numClass = 'odd' %}

{% for item in topSection.items %}
    {% if item.menuType != 'section' and item.active == 1 and loop.index - currentIndex >= 0 and doLoop == true %}
        {% set count = count + 1 %}
    {% elseif item.menuType == 'section' and loop.index - currentIndex >= 0 %}
        {% set doLoop = false %}
    {% endif %}
{% endfor %}

{% if count % 2 == 0 %}
    {% set numClass = 'even' %}
{% endif %}

<ol class="menu-items1 {{ numClass }} bk{{ count }}-columns">